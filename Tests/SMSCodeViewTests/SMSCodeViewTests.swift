    import XCTest
    @testable import SMSCodeView

    final class SMSCodeViewTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SMSCodeView().text, "Hello, World!")
        }
    }
